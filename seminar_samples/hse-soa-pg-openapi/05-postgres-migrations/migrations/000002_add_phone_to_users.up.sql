-- Миграция 2: Добавление поля телефона к пользователям
-- Это пример того, как развивается схема БД со временем
-- Файл: 000002_add_phone_to_users.up.sql

ALTER TABLE users
    ADD COLUMN phone VARCHAR(20);

-- Комментарий: поле опциональное (NULL допустим), без DEFAULT
-- Существующие строки получат phone = NULL
