services:
  users-service:
    build: .
    ports:
      - "8000:8000"
    # Для разработки: автоперезагрузка при изменении файлов
    # command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    # volumes:
    #   - .:/app
